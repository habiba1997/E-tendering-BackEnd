import { juggler } from '@loopback/repository';
export declare class ETenderingDatabaseDataSource extends juggler.DataSource {
    static dataSourceName: string;
    constructor(dsConfig?: object);
}
